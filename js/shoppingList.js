    // // Check if the array contains at least one object
    // if (a.some(
    //         item => typeof item === 'object' 
    //         && item !== null 
    //         && !Array.isArray(item))
    //     ){
    //     return 'Pirmoji reikšmė, turi būti masyvas ir turi turėti objektą/us';
    // }
    // // Check if the array contains at least one non-empty object
    // if (a.some(
    //         item => typeof item === 'object' 
    //         && item !== null 
    //         && !Array.isArray(item) 
    //         && Object.keys(item).length > 0)
    //     )